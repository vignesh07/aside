{
  "name": "@aside/menubar",
  "version": "0.1.0",
  "private": true,
  "description": "macOS menubar frontend for aside — reuses the same TS core as the TUI.",
  "type": "module",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && electron dist/main.js"
  },
  "devDependencies": {
    "electron": "^31.0.0",
    "typescript": "^5.7.0"
  }
}
