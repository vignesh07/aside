<?xml version="1.0" encoding="UTF-8"?>
<!--
  Entitlements for Electron's helper processes (Renderer, GPU, Plugin).

  The helpers inherit the parent's sandbox decision rather than declaring their
  own, but they still need the JIT permissions in their own right: the renderer
  is where V8 actually runs.
-->
<plist version="1.0">
<dict>
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>
  <key>com.apple.security.cs.allow-dyld-environment-variables</key>
  <true/>
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>
  <key>com.apple.security.inherit</key>
  <true/>
</dict>
</plist>
