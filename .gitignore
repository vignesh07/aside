node_modules/
dist/
*.log
.DS_Store

# Local credentials and service state must never enter a public checkout.
.env
.env.*
!.env.example
!.env.*.example
.npmrc
.railway/
*.pem
*.key
*.p8
*.p12
*.pfx
*.cer
*.crt
*.mobileprovision
*.provisionprofile

# npm pack artifacts
*.tgz

# Electron bundle + packaged apps (hundreds of MB; rebuilt by `npm run dist`).
# The generated icons under menubar/assets ARE committed: electron-builder needs
# icon.icns at build time, and regenerating them requires an Electron run.
menubar/build/
menubar/release/
